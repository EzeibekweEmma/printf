#ifndef _MAIN_H_
#define _MAIN_H_

/* including standard library */
#include <stdio.h>
#include <unistd.h>
#include <stdarg.h>

/* Prototypes */
int _printf(const char *format, ...);

#endif /* _MAIN_H_ */
